# python-diary-api
A restful backend API for a diary application that allows users to create, read and delete diary entries. The application uses a database to persist entries and demonstrate basic CRUD operations, backend architecture and API dseign.
